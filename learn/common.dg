(make visible objects handled)
    (exhaust) {
        *($Obj is #in $Room)
        << $Obj >>
        (room $Room)
        ~(pristine descr $Obj)
        ~($Obj is hidden)
        << Making Visible >>
        (now) ($Obj is handled) %% To appear in room descriptions
    }

(intro)
    (make visible objects handled)
    (banner)
    (try [look])
     
#player
(current player *)

(story author)  Howard M. Lewis Ship

(appearance ($Obj is pristine) #in (room $Room))
    (pristine descr $Obj)
    (line)    